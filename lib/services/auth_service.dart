import 'dart:js';

import 'package:flutter/cupertino.dart';

class AuthService{
  handleAuthState(){
    return StreamBuilder(builder: (context,snapsho){

      if(snapsho.hasData){
        return HomeScre
      }
    })
  }
}